# ---------- 依赖 ----------
node_modules/

# ---------- 构建产物 ----------
# 前端产物（vite build 生成；Tauri 打包时会内嵌进 exe，不入库）
dist/
# Rust / Tauri 构建产物（含 release、bundle 安装包、缓存，体积很大）
src-tauri/target/
# tauri-build 每次构建重新生成的权限 schema
src-tauri/gen/schemas/

# ---------- 环境变量与本地配置 ----------
.env
.env.*
!.env.example

# ---------- 编辑器 / 操作系统 ----------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
.DS_Store
Thumbs.db

# ---------- 日志与临时文件 ----------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- 刻意不忽略：以下文件必须提交 ----------
# Cargo.lock         应用类项目须锁定 Rust 依赖版本
# package-lock.json  锁定前端依赖版本
# src-tauri/icons/   打包所需的图标资源
# app-icon.svg       图标源文件，用于 `tauri icon` 重新生成图标
